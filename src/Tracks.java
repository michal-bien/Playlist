public abstract class Tracks {
    protected String title;
    protected String format;
    protected String length;

    public Tracks(String title, String format, String length){
        this.title = title;
        this.format = format;
        this.length = length;
    }

}
